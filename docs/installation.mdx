---
title: "Installation"
description: "Install Grain Analytics in your project"
---

<Note>
  **Looking for a quick start guide?** Check out our [platform-specific quick starts](/quickstart) for step-by-step setup instructions.
</Note>

## Choose Your Installation Method

Pick the method that fits your setup:

<CardGroup cols={2}>
  <Card title="npm / yarn / pnpm" icon="box" href="#package-manager">
    For apps with a build step
  </Card>
  <Card title="CDN" icon="globe" href="#cdn-browser">
    For static sites or quick prototyping
  </Card>
</CardGroup>

---

## Package Manager

Install via npm, yarn, or pnpm:

<CodeGroup>

```bash npm
npm install @grainql/analytics-web
```

```bash yarn
yarn add @grainql/analytics-web
```

```bash pnpm
pnpm add @grainql/analytics-web
```

</CodeGroup>

The package includes:
- Core analytics SDK
- Remote configuration
- React hooks (optional)
- Full TypeScript support

### Import and Use

```typescript
// Vanilla JS or any framework
import { createGrainAnalytics } from '@grainql/analytics-web';

// React hooks
import { GrainProvider, useTrack, useConfig } from '@grainql/analytics-web/react';
```

<Tip>
  **Next steps**: Check out the [React Quick Start](/quickstart/react) or [Vanilla JS Quick Start](/quickstart/vanilla-js) for complete examples.
</Tip>

---

## CDN (Browser)

For sites without a build step, use the Grain Tag CDN:

```html
<script src="https://tag.grainql.com/v4/your-tenant-id.js"></script>
<script>
  // SDK auto-initializes, get the instance
  const grain = GrainTag.getInstance();
  
  // Track custom events
  grain.track('button_clicked', { button_id: 'signup' });
</script>
```

Replace `your-tenant-id` with your tenant identifier (not UUID).

### Version Pinning

For production, pin to a specific version:

```html
<script src="https://unpkg.com/@grainql/analytics-web@1.0.0/dist/index.global.js"></script>
```

Check the [npm package](https://www.npmjs.com/package/@grainql/analytics-web) for available versions.

### Alternative CDNs

```html
<!-- jsDelivr -->
<script src="https://cdn.jsdelivr.net/npm/@grainql/analytics-web/dist/index.global.js"></script>

<!-- unpkg -->
<script src="https://unpkg.com/@grainql/analytics-web/dist/index.global.js"></script>
```

<Tip>
  **Next steps**: See the [CDN Quick Start](/quickstart/cdn) for complete examples.
</Tip>

---

## Platform Support

Grain Analytics works across multiple environments:

### Browser
- ✅ Chrome, Firefox, Safari, Edge
- ✅ Modern ES6+ and legacy ES5
- ✅ Module bundlers (Vite, Webpack, esbuild)

### Frameworks
- ✅ React (hooks included)
- ✅ Next.js (App Router & Pages Router)
- ✅ Vue, Svelte, Angular
- ✅ Vanilla JavaScript

### Server
- ✅ Node.js 14+
- ✅ Serverless (AWS Lambda, Vercel, Netlify)
- ✅ React Native

---

## Build Formats

The package includes multiple formats:

- **ESM** (`dist/index.mjs`) — Modern bundlers
- **CommonJS** (`dist/index.js`) — Node.js
- **IIFE** (`dist/index.global.js`) — Browsers via CDN
- **TypeScript** (`dist/index.d.ts`) — Type definitions

Your bundler automatically picks the right format.

---

## TypeScript Support

Full TypeScript support out of the box—no additional packages needed:

```typescript
import { createGrainAnalytics, GrainConfig, GrainEvent } from '@grainql/analytics-web';

const config: GrainConfig = {
  tenantId: 'your-tenant-id',
  authStrategy: 'NONE',
  batchSize: 50
};

const grain = createGrainAnalytics(config);
```

---

## Bundle Size

Grain is designed to be lightweight:

- **Core SDK**: ~6 KB (gzipped)
- **With React Hooks**: ~8 KB (gzipped)
- **Zero dependencies**: No bloat

The SDK is tree-shakeable—you only bundle what you use.

---

## Verify Installation

Test that everything works:

```typescript
import { createGrainAnalytics } from '@grainql/analytics-web';

const grain = createGrainAnalytics({
  tenantId: 'your-tenant-id',
  debug: true // Enable console logging
});

grain.track('installation_test', {
  timestamp: new Date().toISOString()
});

console.log('Grain Analytics initialized!');
```

Check your browser console to see batching and sending activity.

---

## Privacy & GDPR

Grain includes built-in privacy features:

### Basic Setup

```typescript
const grain = createGrainAnalytics({
  tenantId: 'your-tenant-id',
  consentMode: 'opt-in',        // Require consent
  waitForConsent: true,          // Queue events until consent
  enableCookies: true,           // Use cookies after consent
  anonymizeIP: true              // Anonymize IP addresses
});
```

### Manage Consent

```typescript
// Grant consent
grain.grantConsent(['necessary', 'analytics']);

// Revoke consent
grain.revokeConsent();

// Check consent
const hasConsent = grain.hasConsent('analytics');
```

### React Consent Banner

```tsx
import { GrainProvider, ConsentBanner } from '@grainql/analytics-web/react';

function App() {
  return (
    <GrainProvider config={{ 
      tenantId: 'your-tenant-id',
      consentMode: 'opt-in'
    }}>
      <ConsentBanner 
        position="bottom"
        theme="glass"
        privacyPolicyUrl="/privacy"
      />
      {/* Your app */}
    </GrainProvider>
  );
}
```

<Info>
  **GDPR Compliance**: In opt-in mode without consent, only ephemeral session IDs are used. No cookies or persistent tracking until consent is granted.
</Info>

---

## What's Next?

<CardGroup cols={2}>
  <Card title="Quick Start Guides" icon="rocket" href="/quickstart">
    Platform-specific setup instructions
  </Card>
  <Card title="Authentication" icon="key" href="/authentication">
    Set up Auth0 or custom JWT authentication
  </Card>
  <Card title="Core Concepts" icon="book" href="/core/event-tracking">
    Learn about event tracking and remote config
  </Card>
  <Card title="React Hooks" icon="react" href="/react/overview">
    Use Grain with React
  </Card>
</CardGroup>
